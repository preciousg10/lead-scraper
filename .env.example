# Google Places API key — used by search.py for the Places Text Search API.
# Get one at https://console.cloud.google.com/ (enable the Places API).
# Set it in your environment before running:  export GOOGLE_PLACES_API_KEY=...
GOOGLE_PLACES_API_KEY=your-google-places-api-key-here
